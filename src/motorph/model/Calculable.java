package motorph.model;

// Interface for objects that can calculate a value based on an amount.
// Demonstrates Abstraction through interface usage.
public interface Calculable {
    double calculate(double amount);
}
